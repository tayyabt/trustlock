/**
 * npm lockfile parser — v1, v2, v3.
 *
 * STUB — full implementation in F02-S03.
 *
 * @param {string} _lockfileContent  - Raw lockfile JSON string.
 * @param {string} _packageJsonContent - Raw package.json JSON string.
 * @returns {ResolvedDependency[]}
 */
export function parseNpm(_lockfileContent, _packageJsonContent) {
  // Placeholder seam. Replaced in F02-S03.
  return [];
}
